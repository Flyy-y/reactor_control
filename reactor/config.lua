return {
    reactor_id = 1,
    server_channel = 100,
    broadcast_channel = 101,
    
    update_interval = 2,
    heartbeat_interval = 15,
    
    emergency_shutdown = {
        temperature = 1200,
        damage = 5,
        waste_percent = 5
    },
    
    display = {
        enabled = true,
        update_interval = 0.5
    }
}